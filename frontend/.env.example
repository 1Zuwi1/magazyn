NEXT_PUBLIC_API_URL="" # Public domain of the application api. For example api.domain.com
INTERNAL_API_URL=http://localhost:8080 # Internal URL of API (used if frontend and backend are on the same server) - leave blank to not use
PORT=3001 # Port the app listens on; must stay in sync with the Dockerfile, which currently uses 3001. If you change this, also update the Dockerfile and any container config to use the same port.